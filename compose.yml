services:
    coquo:
        container_name: coquo-next
        image: coquo
        ports:
            - 3030:3000
        volumes:
            - ./config:/config
        environment:
            - AUTH_SECRET="define_auth_secret"
