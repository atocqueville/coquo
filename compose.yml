services:
    coquo:
        container_name: coquo-next
        image: coquo
        ports:
            - 3030:3000
        volumes:
            - ./config:/config
        # environment:
        #   - BETTER_AUTH_SECRET=...   # requis en prod
        #   - OAUTH_AUTH_URL=https://coquo.example.com  # si Google auth activé
        #   - GOOGLE_CLIENT_ID=...
        #   - GOOGLE_CLIENT_SECRET=...
